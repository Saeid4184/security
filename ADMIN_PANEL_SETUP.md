# پنل مدیریت وب — راه‌اندازی میزبانی (Hosting)

فایل `docs/index.html` پنل مدیریتی است که حالا مستقیماً و به‌صورت زنده به همان دیتابیس Firebase که اپلیکیشن اندروید استفاده می‌کند وصل می‌شود — **نه داده‌ی نمایشی، نه فایل CSV دستی**.

⚠️ قبل از این مرحله، حتماً مراحل `FIREBASE_SETUP.md` را کامل کرده باشید (فعال‌سازی Firestore، Authentication، و قوانین امنیتی)، وگرنه این پنل چیزی نشان نمی‌دهد یا خطای دسترسی می‌دهد.

## مرحله ۱: فعال‌سازی GitHub Pages

1. وارد ریپازیتوری اپلیکیشن‌تان در github.com شوید.
2. تب **Settings** (بالای صفحه ریپو) را باز کنید.
3. از منوی سمت چپ روی **Pages** کلیک کنید.
4. زیر «Build and deployment» → «Source»، گزینه **Deploy from a branch** را انتخاب کنید.
5. شاخه (Branch) را روی **main** بگذارید و پوشه را روی **/docs** تنظیم کنید (نه `/root`).
6. **Save** بزنید.
7. چند ثانیه تا یک دقیقه صبر کنید؛ صفحه را رفرش کنید تا آدرس سایت شما (چیزی شبیه `https://saeid4184.github.io/REPO-NAME/`) نمایش داده شود.

## مرحله ۲: وارد کردن آدرس در اپلیکیشن

1. اپلیکیشن را باز کنید → با حساب **مدیر** وارد شوید → آیکون تنظیمات (⚙️) → بخش «پنل مدیریت وب».
2. آدرسی که از GitHub Pages گرفتید را در کادر وارد کنید و **ثبت** را بزنید.
3. از حالا، آیکون 🌐 در نوار بالای صفحه اصلی، این پنل را در مرورگر گوشی باز می‌کند.

## ورود به پنل

پنل با **همان حساب‌های واقعی** که در اپلیکیشن اندروید ساخته‌اید کار می‌کند (ایمیل/رمز عبور Firebase) — نه یک یوزر/پسورد ثابت. فقط حساب‌هایی که نقش‌شان «مدیر» است اجازه‌ی ورود به این پنل را دارند؛ نگهبانان با همان حساب خودشان نمی‌توانند وارد پنل وب شوند (فقط اپلیکیشن اندروید).

## داده‌های نمایش‌داده‌شده

- **زنده و واقعی هستند** — همان لحظه که یک نگهبان در گوشی‌اش ثبت ورود/خروج می‌زند، این پنل (بدون رفرش کردن صفحه) به‌روز می‌شود.
- ستون «آخرین ثبت‌کننده» نشان می‌دهد کدام نگهبان آخرین رویداد آن فرد/ماشین را ثبت کرده — دقیقاً همان چیزی که برای پاسخ‌گویی و شفافیت لازم بود.
- خروجی CSV این پنل (دکمه «دانلود مستقیم فایل اکسل») یک عکسِ لحظه‌ای از وضعیت فعلی است، نه تاریخچه‌ی کامل — برای گزارش تاریخی کامل، از خود اپلیکیشن اندروید (صفحه «گزارش‌ها») با بازه‌ی تاریخی دلخواه خروجی اکسل بگیرید.

## کلید API هوش مصنوعی در این پنل

تب «دستیار هوشمند Gemini» داخل این پنل یک کادر مجزا برای کلید Gemini دارد (جدا از کلیدی که در تنظیمات اپلیکیشن اندروید وارد کرده‌اید). چون این صفحه در مرورگر اجرا می‌شود، هر کلیدی که آنجا وارد کنید در درخواست‌های شبکه قابل مشاهده است — پس یک کلید جداگانه و کم‌ریسک برای این پنل در نظر بگیرید، نه کلید حساس.
