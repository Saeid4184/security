# راه‌اندازی Firebase (همگام‌سازی ابری بین ۵ گوشی + پنل مدیر)

این مراحل را **فقط یک‌بار** باید انجام دهید. بعد از این، همه‌ی گوشی‌های نگهبانی و پنل وب به‌طور خودکار با هم همگام می‌مانند.

## مرحله ۱: فعال‌سازی Cloud Firestore

1. به [console.firebase.google.com](https://console.firebase.google.com) بروید و پروژه‌ی `ir-factory-gate` را باز کنید (همانی که `google-services.json` از آن گرفته شده).
2. از منوی سمت چپ: **Build → Firestore Database**.
3. **Create database** را بزنید.
4. حالت **Production mode** را انتخاب کنید (نه Test mode — قوانین امنیتی خودمان را در مرحله بعد وارد می‌کنیم).
5. یک منطقه (Location) نزدیک به ایران انتخاب کنید (مثلاً `europe-west3` یا `eur3`) و **Enable** بزنید.

## مرحله ۲: فعال‌سازی ورود با ایمیل/رمز عبور

1. از منوی سمت چپ: **Build → Authentication**.
2. **Get started** را بزنید.
3. از لیست ارائه‌دهندگان (Sign-in providers)، روی **Email/Password** کلیک کنید.
4. گزینه‌ی اول (Email/Password) را **Enable** کنید و **Save** بزنید.

## مرحله ۳: وارد کردن قوانین امنیتی (Security Rules)

⚠️ این مرحله را رد نکنید — بدون آن، دیتابیس شما یا کاملاً بسته می‌ماند (هیچ‌کس نمی‌تواند ثبت کند) یا اگر در حالت تست بمانید، کاملاً باز و ناامن است.

1. در همان صفحه‌ی Firestore Database، تب **Rules** را باز کنید.
2. محتوای فایل `firestore.rules` (کنار همین راهنما در پروژه) را کپی کرده و به‌جای متن موجود جای‌گذاری کنید.
3. **Publish** را بزنید.

خلاصه‌ی این قوانین:
- فقط کاربرانی که وارد حساب شده‌اند (نگهبان یا مدیر) می‌توانند بخوانند/بنویسند.
- رکوردهای تاریخچه‌ی ورود/خروج (`logs`) بعد از ثبت، غیرقابل ویرایش یا حذف هستند (برای جلوگیری از دستکاری سوابق).
- فقط خود شخص یا یک مدیر می‌تواند نقش (نگهبان/مدیر) یک حساب را تغییر دهد.

## مرحله ۴: ساخت اولین حساب (خودکار مدیر می‌شود)

1. اپلیکیشن اندروید را (بعد از build گرفتن با تغییرات جدید) روی یکی از گوشی‌ها نصب کنید.
2. در صفحه‌ی ورود، **«حساب ندارید؟ ثبت‌نام کنید»** را بزنید.
3. نام، ایمیل، و رمز عبور دلخواه (حداقل ۶ کاراکتر) وارد کنید و ثبت‌نام کنید.
4. **اولین حسابی که تا‌به‌حال در این پروژه ساخته می‌شود، به‌طور خودکار نقش «مدیر» می‌گیرد.** حساب‌های بعدی به‌طور پیش‌فرض «نگهبان» هستند.

## مرحله ۵: ساخت حساب برای بقیه‌ی نگهبانان

هر نگهبان باید همین مسیر (ثبت‌نام) را طی کند و یک حساب مجزا با ایمیل خودش بسازد — همه به‌طور پیش‌فرض نقش «نگهبان» می‌گیرند مگر اینکه شما (به‌عنوان مدیر) نقش‌شان را تغییر دهید.

### تبدیل یک نگهبان به مدیر (در آینده)
1. در کنسول Firebase: **Build → Authentication** → لیست کاربران → پیدا کردن ایمیل فرد مورد نظر → کپی کردن **User UID** او.
2. **Build → Firestore Database → Data** → مجموعه‌ی `users` → سند (Document) با همان UID.
3. فیلد `role` را از `"GUARD"` به `"ADMIN"` تغییر دهید و ذخیره کنید.

## مرحله ۶: اتصال پنل وب

فایل `docs/index.html` از قبل با تنظیمات همین پروژه Firebase پیکربندی شده. اگر بعد از انتشار روی GitHub Pages (طبق `ADMIN_PANEL_SETUP.md`) پیغام خطای اتصال دیدید:

1. در کنسول Firebase: **⚙️ Project settings** (بالای صفحه، کنار Project Overview).
2. پایین صفحه، بخش **Your apps** → **Add app** → آیکون وب (`</>`).
3. یک نام دلخواه بدهید (مثلاً «پنل مدیریت») و **Register app** بزنید.
4. کدی که نمایش داده می‌شود (شامل `apiKey`, `authDomain`, `projectId`, و ...) را کپی کنید.
5. در فایل `docs/index.html`، بخش `const firebaseConfig = { ... }` را با همین مقادیر جایگزین کنید.

## نکات مهم

- **عکس‌های پروفایل پرسنل/ماشین‌آلات هم‌زمان‌سازی نمی‌شوند** — چون این عکس‌ها از گالری خود گوشی انتخاب می‌شوند، فقط روی همان گوشی معتبرند. یعنی اگر عکسی برای یک نفر در گوشی الف بگذارید، در گوشی ب دیده نمی‌شود. اگر بعداً خواستید این را هم همگام کنید (با آپلود واقعی روی Firebase Storage)، بگویید تا اضافه‌اش کنم.
- **بدون اینترنت چه می‌شود؟** اگر گوشی یک نگهبان لحظه‌ای آفلاین شود، ثبت‌های او محلی ذخیره می‌مانند و به‌محض وصل شدن دوباره، خودکار با بقیه هماهنگ می‌شوند (این قابلیت داخلی خود Firestore است، نیازی به تنظیم اضافه ندارد).
- **هزینه:** در حجم یک کارخانه (چند صد رویداد در روز)، این کاملاً داخل سقف رایگان (Spark Plan) فایربیس می‌ماند.
