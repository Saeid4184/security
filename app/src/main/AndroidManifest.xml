<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".FactoryApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConcreteFactory">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.CheckpointPickerActivity"
            android:exported="false"
            android:label="@string/checkpoint_picker_title" />

        <activity
            android:name=".ui.MyLogsActivity"
            android:exported="false"
            android:label="@string/my_logs_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ui.CategoryDetailActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".ui.GlobalSearchActivity"
            android:exported="false"
            android:label="@string/global_search_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.InspectionFormActivity"
            android:exported="false"
            android:label="@string/inspection_form_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.ReportFormActivity"
            android:exported="false"
            android:label="@string/report_form_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.AdminDashboardActivity"
            android:exported="true"
            android:label="@string/dashboard_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.OpenDefectsActivity"
            android:exported="false"
            android:label="@string/open_defects_title"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.FleetHeatmapActivity"
            android:exported="false"
            android:label="@string/fleet_heatmap_title"
            android:parentActivityName=".ui.AdminDashboardActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
